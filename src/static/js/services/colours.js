/*
 * Mood of Story == Colour of Cup
 * 
 */


function picker() {
  // hue picker for the humans (`e` and `f`)

}

function shader() {
  // `picker()` -> hue; `depth^` -> saturation; `reads` -> luminosity

}

function domain() {
  // story picker on `<persona>.tampopo.eth`

}
