/*
 * This code converts your browser into a node
 * 
 * i. `optimism` locally enables the optimistic rollup functionality
 * ii. `snarkyaf` submits a zk-SNARK proof to the network, by minting ERC-1155
 * iii. `goerlier` enables a persona to pay forward, ie, submit other queued batches
 * 
 */


function optimism() {
  // 

}

function snarkyaf() {
  // 

}

function goerlier(val=0) {
  // 
  if (!val) return

}