/*
 * This code runs if no cookies (read: personas) are found locally
 * 
 * i. Informs user of site functionality
 * ii. gets user choice for specific services, in the interest of better UX
 * iii. creates a new persona using subdomains of `tampopo.eth`
 * iv. Mints the persona to name, also makes persona _real_
 * 
 */

function educate() {
  // Inform user of site functionality and user involvement

}

function cookies() {
  // Get informed, specific consent by user to proceed

}

function persona() {
  // Create a private key and persona on `tampopo.eth`

}

function support(val=0) {
  // Mint persona by sumitting batch, created in `persona()`
  if (!val) return

}